diff --git a/prctl.c b/prctl.c
index befa14b..28f4145 100644
--- a/prctl.c
+++ b/prctl.c
@@ -286,7 +286,7 @@ SYS_FUNC(prctl)
 
 	case PR_SET_SECCOMP:
 		tprints(", ");
-		printxval64(seccomp_mode, arg2,
+		printxval64(xlat_seccomp_mode, arg2,
 			    "SECCOMP_MODE_???");
 		if (SECCOMP_MODE_STRICT == arg2)
 			return RVAL_DECODED;
diff --git a/xlat/seccomp_mode.h b/xlat/seccomp_mode.h
index 842dd44..4152df9 100644
--- a/xlat/seccomp_mode.h
+++ b/xlat/seccomp_mode.h
@@ -16,7 +16,7 @@
 #else
 
 static
-const struct xlat seccomp_mode[] = {
+const struct xlat xlat_seccomp_mode[] = {
  XLAT(SECCOMP_MODE_DISABLED),
  XLAT(SECCOMP_MODE_STRICT),
  XLAT(SECCOMP_MODE_FILTER),
