include:
  - project: devops/iopsyswrt
    file: top.yml

EX400:
  extends: .supported_board
  variables:
    SKIP_TEST: "true"

SMARTHUB3:
  extends: .supported_board

DISC:
  extends: .supported_board
  variables:
    SKIP_TEST: "true"

EAGLE:
  extends: .supported_board

PANTHER:
  extends: .supported_board

PANDA:
  extends: .supported_board

ZEBRA:
  extends: .supported_board
  variables:
    SKIP_TEST: "true"

EN7562:
  extends: .supported_board
